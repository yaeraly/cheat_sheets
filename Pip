# Pip Cheat Sheet
|Freeze|Description|
|---|---|
|pip freeze|Generate output suitable for a requirements file.|
|pip freeze > requirements.txt|Generate a requirements file with environment's current package list|
|pip install -r requirements.txt| Install packages from "requirements.txt" in another environment
